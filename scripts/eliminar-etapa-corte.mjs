// Temporariamente desativado para preservar a estabilidade do build.
// A remoção da etapa Corte será implementada diretamente no App.jsx,
// em vez de alterar o código da aplicação durante o build.
console.log("NeoCooler: script de eliminação da etapa Corte desativado; build seguro.");
