import fs from "node:fs";

const path = "src/App.jsx";
let source = fs.readFileSync(path, "utf8");
let alterado = false;

const aplicar = (nome, from, to) => {
  if (!source.includes(from)) {
    console.log(`NeoCooler: alvo não encontrado para ${nome}.`);
    return;
  }
  source = source.replace(from, to);
  alterado = true;
  console.log(`NeoCooler: ${nome} aplicado.`);
};

// Estado temporário usado para editar uma alocação já lançada sem salvar a cada tecla digitada.
aplicar(
  "estadoEdicaoSublimacao",
  '  const [alocForm, setAlocForm] = useState({}); // { "pedido||produto": {cor, qtd, sublimador, data} }',
  '  const [alocForm, setAlocForm] = useState({}); // { "pedido||produto": {cor, qtd, sublimador, data} }\n  const [edicaoSublimacao, setEdicaoSublimacao] = useState({}); // { id: {cor, qtd, sublimador, data} }'
);

// Ao enviar várias vezes a mesma cor para o mesmo sublimador/data, soma no registro existente.
const antigoEnviar = `  const enviarParaSublimacao = (pedidoNum, produtoNome) => {\n    const f = getAlocForm(pedidoNum, produtoNome);\n    const qtdNum = Math.max(1, Number(f.qtd) || 1);\n    const novo = {\n      id: uid(),\n      pedido: pedidoNum,\n      produto: produtoNome,\n      cor: f.cor,\n      qtd: qtdNum,\n      etapa: "sublimacao",\n      sublimador: f.sublimador,\n      dataSublimacao: f.data || hoje(),\n      equipe: "Não decidido",\n      feito: false,\n      conferido: false,\n      criadoEm: Date.now(),\n    };\n    salvar([...itens, novo]);\n    setAlocForm((f2) => ({ ...f2, [chaveAloc(pedidoNum, produtoNome)]: { cor: CORES[0].nome, qtd: 1, sublimador: f.sublimador, data: f.data } }));\n  };`;
const novoEnviar = `  const enviarParaSublimacao = (pedidoNum, produtoNome) => {\n    const f = getAlocForm(pedidoNum, produtoNome);\n    const qtdNum = Math.max(1, Number(f.qtd) || 1);\n    const cor = f.cor;\n    const sublimador = f.sublimador;\n    const dataSublimacao = f.data || hoje();\n    const existente = itens.find((i) =>\n      i.etapa === "sublimacao" &&\n      i.pedido === pedidoNum &&\n      i.produto === produtoNome &&\n      i.cor === cor &&\n      (i.sublimador || SUBLIMADORES[0]) === sublimador &&\n      (i.dataSublimacao || "") === dataSublimacao\n    );\n\n    let novaLista;\n    if (existente) {\n      novaLista = itens.map((i) =>\n        i.id === existente.id ? { ...i, qtd: i.qtd + qtdNum } : i\n      );\n    } else {\n      const novo = {\n        id: uid(),\n        pedido: pedidoNum,\n        produto: produtoNome,\n        cor,\n        qtd: qtdNum,\n        etapa: "sublimacao",\n        sublimador,\n        dataSublimacao,\n        equipe: "Não decidido",\n        feito: false,\n        conferido: false,\n        criadoEm: Date.now(),\n      };\n      novaLista = [...itens, novo];\n    }\n    salvar(novaLista);\n    setAlocForm((f2) => ({ ...f2, [chaveAloc(pedidoNum, produtoNome)]: { cor: CORES[0].nome, qtd: 1, sublimador, data: dataSublimacao } }));\n  };\n\n  const getEdicaoSublimacao = (it) => edicaoSublimacao[it.id] || {\n    cor: it.cor || CORES[0].nome,\n    qtd: it.qtd,\n    sublimador: it.sublimador || SUBLIMADORES[0],\n    data: it.dataSublimacao || hoje(),\n  };\n\n  const setEdicaoSublimacaoCampo = (it, campo, valor) => {\n    setEdicaoSublimacao((e) => ({\n      ...e,\n      [it.id]: { ...getEdicaoSublimacao(it), [campo]: valor },\n    }));\n  };\n\n  const salvarEdicaoSublimacao = (id) => {\n    const atual = itens.find((i) => i.id === id && i.etapa === "sublimacao");\n    if (!atual) return;\n    const f = getEdicaoSublimacao(atual);\n    const qtdNum = Math.max(1, Number(f.qtd) || 1);\n    const cor = f.cor || CORES[0].nome;\n    const sublimador = f.sublimador || SUBLIMADORES[0];\n    const dataSublimacao = f.data || hoje();\n    const existente = itens.find((i) =>\n      i.id !== id &&\n      i.etapa === "sublimacao" &&\n      i.pedido === atual.pedido &&\n      i.produto === atual.produto &&\n      i.cor === cor &&\n      (i.sublimador || SUBLIMADORES[0]) === sublimador &&\n      (i.dataSublimacao || "") === dataSublimacao\n    );\n\n    let novaLista;\n    if (existente) {\n      // Se a cor corrigida já existe, os dois registros viram um só.\n      novaLista = itens\n        .filter((i) => i.id !== id)\n        .map((i) => i.id === existente.id ? { ...i, qtd: i.qtd + qtdNum } : i);\n    } else {\n      novaLista = itens.map((i) =>\n        i.id === id\n          ? { ...i, cor, qtd: qtdNum, sublimador, dataSublimacao }\n          : i\n      );\n    }\n    salvar(novaLista);\n    setEdicaoSublimacao((e) => { const n = { ...e }; delete n[id]; return n; });\n  };`;
aplicar("enviarESomarSublimacao", antigoEnviar, novoEnviar);

// Troca a linha somente de leitura da aba Sublimação por uma linha editável.
const antigoItemSub = `                    <div key={it.id} style={styles.itemLinha} className="item-linha">\n                      <button style={styles.enviarBtn} onClick={() => moverParaAguardandoCostura(it.id)}>Enviar p/ costura</button>\n                      <span style={{ ...styles.swatchSm, background: corHex(it.cor) }} />\n                      <span style={styles.itemTexto}>#{it.pedido} · {it.produto} <b>· {it.cor}</b> · {it.qtd}un</span>\n                      <span style={styles.sublimadorPill}>{it.sublimador}</span>\n                      <button style={styles.removerBtn} onClick={() => removerItem(it.id)}>×</button>\n                    </div>`;
const novoItemSub = `                    <div key={it.id} style={styles.itemLinha} className="item-linha">\n                      {(() => {\n                        const f = getEdicaoSublimacao(it);\n                        return (\n                          <>\n                            <span style={{ ...styles.swatchSm, background: corHex(f.cor) }} />\n                            <span style={{ ...styles.itemTexto, minWidth: 120 }}><b>#{it.pedido}</b> · {it.produto}</span>\n                            <select\n                              style={{ ...styles.equipeSelect, borderRadius: 8, padding: "6px 8px", minWidth: 105 }}\n                              value={f.cor}\n                              onChange={(e) => setEdicaoSublimacaoCampo(it, "cor", e.target.value)}\n                              aria-label="Cor da sublimação"\n                            >\n                              {CORES.map((c) => <option key={c.nome} value={c.nome}>{c.nome}</option>)}\n                            </select>\n                            <input\n                              style={{ ...styles.input, width: 82, padding: "6px 8px" }}\n                              type="number"\n                              min={1}\n                              value={f.qtd}\n                              onChange={(e) => setEdicaoSublimacaoCampo(it, "qtd", e.target.value)}\n                              aria-label="Quantidade"\n                            />\n                            <select\n                              style={{ ...styles.equipeSelect, borderRadius: 8, padding: "6px 8px", minWidth: 100 }}\n                              value={f.sublimador}\n                              onChange={(e) => setEdicaoSublimacaoCampo(it, "sublimador", e.target.value)}\n                              aria-label="Sublimador"\n                            >\n                              {SUBLIMADORES.map((s) => <option key={s} value={s}>{s}</option>)}\n                            </select>\n                            <button style={styles.enviarBtn} onClick={() => salvarEdicaoSublimacao(it.id)}>Salvar</button>\n                            <button style={{ ...styles.enviarBtn, background: "#1c2a3a" }} onClick={() => moverParaAguardandoCostura(it.id)}>Enviar p/ costura</button>\n                            <button style={styles.removerBtn} onClick={() => removerItem(it.id)}>×</button>\n                          </>\n                        );\n                      })()}\n                    </div>`;
aplicar("edicaoVisualSublimacao", antigoItemSub, novoItemSub);

if (alterado) {
  fs.writeFileSync(path, source, "utf8");
  console.log("NeoCooler: agrupamento e edição de alocações de sublimação habilitados.");
} else {
  console.log("NeoCooler: nenhuma alteração necessária.");
}
